import type { LoginPayload } from '../types';

export const login = async (_payload: LoginPayload): Promise<void> => {
  await Promise.resolve();
};

export const loginWithFaceId = async (): Promise<void> => {
  await Promise.resolve();
};
